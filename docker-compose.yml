version: "3.8"

services:
  clipsync:
    build: .
    container_name: clipsync
    restart: unless-stopped
    ports:
      - "5555:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - DATABASE_URL=postgresql://Yellowkid:Moss9pep2828@192.168.1.177:2665/clipsync
      - JWT_SECRET=clipsync-prod-jwt-8k3m9x2v7n
      - OPENAI_API_KEY=sk-placeholder
      - OPENAI_MODEL=gpt-4o-mini
      - BLOB_STORAGE_PATH=/app/storage/blobs
    volumes:
      - clipsync-blobs:/app/storage/blobs
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  clipsync-blobs:
